# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Sales team email for lead notifications
SALES_EMAIL=vendas@arxisvr.com

# Server Configuration
PORT=3001

# Instructions:
# 1. For Gmail: 
#    - Enable 2-factor authentication
#    - Generate an App Password: https://myaccount.google.com/apppasswords
#    - Use the generated password in SMTP_PASS
#
# 2. For other providers (SendGrid, Mailgun, etc):
#    - Update SMTP_HOST and SMTP_PORT accordingly
#    - Use API credentials if required
#
# 3. Update SALES_EMAIL with your sales team email address
