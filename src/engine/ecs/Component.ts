/**
 * Base Component class for ECS
 * Components are pure data structures attached to entities
 */
export abstract class Component {
  // Base class for all components
  // Subclasses should define their data properties
}